mutation IncludeParkingSessionInBIR($id: String!) {
  includeParkingSessionInBIR(id: $id) {
    id
    vehicleType
    plateNumber
    enteredAt
    exitedAt
    durationMinutes
    parkingFee
    parkingState
    paymentStatus
    includeInBIRReport
  }
}
